package 배열;

public class Memo {
	/*
	 * 배열을 사용할 때 고려 해야 할 점.
	 * 
	 * 1. 이미 값들을 알고 있는 경우
	 * 			int[] num = {1,2,3};
	 * 			배열 = (고정적 크기, 삭제, 추가, 변경 불가능)
	 * 
	 * 2. 값들을 처음에 알지 못하는 경우
	 * 			int[] num = new int[3];
	 * 
	 */
}
