tomcat 8.5 다운로드 시 마지막에 긴글자 다운로드


톰켓 설정시 이름 다 변경 후 톰켓 실행

실행 돼 있던 톰켓 연결 끊고 다시 연결


모든 사람에게 똑같이 나타나는 페이지는 정적페이지 => html
사람마다 다르게 바뀌는 페이지는 동적페이지

톰켓에 있는 자바를 실행시키는 것이 들어가 있는데
그 이름은 container 혹은 engine 이라고 한다.


브라우저(클라이언트) --> 서버(web)--> DB
			  request	  request
브라우저(클라이언트) <-- server <--DB
			response

			
동적 페이지는 웹 서버에서 실행이 불가능 하므로, 자바를 통해 DAO를 이용해 동적 페이지를 실행시킨다.
톰켓에는 자바의 역할을 하는 것이 있는데 그것을 엔진(engine) 또는 컨테이너(container)라고 한다.			