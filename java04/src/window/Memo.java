package window;

public class Memo {
		/* switch ~ case는
		 * 조건에 맞으면 실행하고,
		 * break가 없으면 아래의 조건도 다 실행해 버린다.
		 * 그래서 우리가 break룰 걸어 주어야 한다.
		 * 
		 * 비교) if는 조건에 맞으면 처리하고 바로 break
		 * 
		 * 변수의 값의 상태값 3가지
		 * ----------------
		 * 1.쓰레기값 ex) String result;
		 * 2.초기 값 ex) String result = "";, String result = null;
		 * 3.대입 값 ex) String result = "ㅇㅇ";
		 * 
		 */
}
